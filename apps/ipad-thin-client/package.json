{
  "name": "denis-ipad-thin-client",
  "version": "1.0.0",
  "description": "Denis Thin Client — WKWebView + AVAudioSession background audio",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "cap:init": "npx cap init 'Denis Voice' so.denis.voice.client --web-dir dist",
    "cap:add": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios",
    "build:ios": "npm run build && npm run cap:sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.0.0"
  }
}
